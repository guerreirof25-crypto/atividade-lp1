#include <stdio.h>

void main(void){
    int soma=0;
    int n=1;
    while (n!=0){
        printf("digite o número(0 para parar): ");
        scanf("%d", &n);
        soma+=n;
    }
    printf("a soma é: %d\n", soma);
}
